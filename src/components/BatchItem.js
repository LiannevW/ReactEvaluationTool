import React, { PureComponent } from 'react'

class BatchItem extends PureComponent {
  render() {

    return(
        <div>
          {this.props}
        </div>

    )
  }
}

export default BatchItem
